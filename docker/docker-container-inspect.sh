#!/bin/bash

##      -- docker container inspect --     ##
    
    # Muestra metadata de los contenedores especificados (volumes, startup config networking, etc...)

docker container run inspect visitor