#!/bin/bash

##      -- docker network disconnect NETWORK CONTAINER --     ##
    
    # Desconecta el container especificado de la network especificada

docker network disconnect bridge visitor